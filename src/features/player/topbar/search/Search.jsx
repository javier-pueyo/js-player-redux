import { useEffect } from "react";
import { useDispatch } from "react-redux";

export const Search = () => {
    const dispatch = useDispatch()

    useEffect(() => {
        // TODO: use search api
    }, [dispatch])

    return <div></div>
}